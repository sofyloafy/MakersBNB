import NodeEnvironment from "jest-environment-node";
import * as webdriverio from "webdriverio";
declare global {
    namespace NodeJS {
        interface Global {
            browser: typeof browser;
            $: typeof $;
            $$: typeof $$;
            remote: typeof webdriverio.remote;
        }
    }
}
export declare type Options = ConstructorParameters<typeof NodeEnvironment>[0];
export declare type TestEnvironmentOptions = Parameters<typeof webdriverio["remote"]>[0];
export default class WebdriverIOEnvironment extends NodeEnvironment {
    options: TestEnvironmentOptions;
    constructor(options: Options);
    setup(): Promise<void>;
    teardown(): Promise<void>;
}
